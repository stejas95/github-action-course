name: 04-using actions

on: push

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout Code
              uses: actions/checkout@v4
            - name: Printing folders
              run: ls

